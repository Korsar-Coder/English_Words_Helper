services:
  main_app:
    build: .
    ports:
      -"8000:8000"
    depends_on:
      - translator
  
  translator:
    image: libretranslate/libretranslate:latest
    ports:
      -"5000:5000"
    environment:
      - LT_LOAD_ONLY=en,ru